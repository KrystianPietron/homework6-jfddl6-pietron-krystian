const INITIAL_STATE = {
    tasks: [],
    task: '',
    isCompleted: false
}

export default (state = INITIAL_STATE, action) => {
    switch (action.type) {

        default:
            return state
    }
}